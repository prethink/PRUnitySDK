# DamageSystem

Модуль описывает создание, модификацию и применение урона к сущностям PRUnitySDK. Основной компонент состояния — `HealthComponent`, а данные об атаке передаются через интерфейс `IDamageProvider`.

## Поток обработки

1. Оружие или другой источник формирует `IDamageProvider`.
2. `IDamageable.TakeDamage()` принимает атакующего, оружие и провайдер урона.
3. Pre-хуки DamageHookEvent изменяют провайдер или отменяют урон.
4. HealthComponent фиксирует здоровье, состояние смерти и DamageOutcome; затем вызываются post-хуки.
5. Локальные события компонента и глобальные `CombatEvents` уведомляют остальные системы.

При использовании хитбокса вызов перенаправляется в `HealthComponent` сущности, указанной в `EntityLink`. Перед передачей вызывается `GetHandledDamage()`, поэтому специализированный хитбокс может добавить собственный модификатор урона.

## Основные типы

| Тип | Назначение |
| --- | --- |
| `DamageData` | Значение, тип, источник, отдача, болевой шок и применённые модификаторы |
| `IDamageProvider` | Источник `DamageData` |
| `DamageBase` | Базовая реализация провайдера |
| `CommonDamage` | Универсальный простой урон |
| `Damages` | Создание урона нужного вида одной строкой |
| `DamageOverTimeCoroutine` | Периодический урон: горение, отравление, кровотечение |
| `DamageResultExtensions` | `IsApplied()` — засчитан ли удар; есть и для `DamageOutcome` |
| `DamageOutcome` | Подробный итог: `AppliedDamage`, `WasHealthReduced`, `WasCritical` |
| `IDamageable` | Контракт объекта, принимающего урон; `TakeDamage` отдаёт `DamageOutcome` |
| `HealthComponent` | Здоровье, смерть, лечение и возрождение сущности |
| `EntityHitBoxBase` | Перенаправление попадания от коллайдера к сущности |
| `UnitHitBox` | Зона тела и множитель урона |
| `DamageResistanceComponent` | Сопротивления сущности различным типам урона |
| `DamageHookEvent` | Изменение или блокировка урона до применения |
| `CombatEvents` | Глобальные события попадания и убийства |

## Настройка сущности

Добавьте `HealthComponent` на тот же `GameObject`, где расположен наследник `EntityBase`. Начальное здоровье при `Start()` устанавливается равным `MaxHealth`.

### Бесконечное здоровье

Флаг `Is Immortal` — про грушу: по ней бьют, она отзывается, сломать её нельзя.

Удар засчитывается **целиком**: результат `Damaged`, в `DamageOutcome` уходит настоящий
урон, поэтому цифра, вспышка и прочие эффекты срабатывают как обычно. Здоровье при этом
не тратится, и сущность не умирает никогда.

Это не то же, что `Is Block Damage`. Заблокированный урон не проходит вовсе — попадание
приходит с `Blocked`, эффекты молчат, и бить по такой сущности бессмысленно.

| Флаг | Удар засчитан | Эффекты | Здоровье | Может умереть |
| --- | --- | --- | --- | --- |
| обычная сущность | да | да | тратится | да |
| `Is Immortal` | да | да | не тратится | нет |
| `Is Block Damage` | нет (`Blocked`) | нет | не тратится | нет |

`OnHealthChange` у бессмертной сущности **не поднимается**: здоровье не менялось, и полоса
над грушей не должна дёргаться от каждого удара. Тем, кому нужен сам факт удара, служит
`OnDamageProcessed` — через него и работают эффекты урона.

Из кода: `health.SetImmortal(true)`.

Для вынесенного на дочерний объект коллайдера добавьте:

- `EntityLink` — автоматически найдёт `EntityBase` в родительских объектах;
- наследника `EntityHitBoxBase`, например `UnitHitBox`;
- ссылку на используемый `Collider` в Inspector.

Полная настройка и пример вызова из raycast-оружия приведены в [`HitBox/README.md`](../HitBox/README.md).

У `UnitHitBox` настройте `HitGroup`, `DamageMultiplier` и при необходимости `IsCritical`. Попадание в голову автоматически получает флаг `Critical`.

Пример множителей в стиле классического шутера:

| Зона | Пример множителя |
| --- | ---: |
| Голова | 4.0 |
| Грудь, руки | 1.0 |
| Живот | 1.25 |
| Ноги | 0.75 |

Это только стартовые значения: фактический баланс задаётся отдельно на каждом хитбоксе в Inspector.

## Нанесение урона

```csharp
DamageOutcome outcome = target.TakeDamage(
    attacker,
    weapon,
    Damages.Bullet(25f, knockBack: 4f));
```

`TakeDamage` возвращает подробный итог, а не одно значение результата: внутри он всё
равно его строит, и отдавать обрезанную версию значило бы заставлять вызывающего лезть
за остальным в `LastDamageOutcome` — а через хитбокс ещё и искать сам `HealthComponent`.

**Никогда не `null`.** Путь, который не дошёл до здоровья, отдаёт `DamageOutcome.NotHandled` —
общий пустой итог. Проверка на `null` у каждого вызова стоила бы дороже.

### Виды урона

Наследников на каждый вид в системе нет, и заводить их не нужно. По классу урона никто
не ветвится: сопротивления, зональные множители и события читают флаги `DamageType`.
А сам `DamageType` комбинируется — один удар бывает разом огненным, взрывным и по
площади, — и класс на вид такого не выразил бы.

Выражает это `Damages`:

```csharp
Damages.Fire(10f);
Damages.Poison(5f);
Damages.Of(30f, DamageType.Fire | DamageType.Explosion, knockBack: 5f);
```

Именованные методы есть на каждый вид из `DamageType`: `Common`, `Fall`, `Bullet`,
`Fire`, `Ice`, `Electric`, `Poison`, `Radiation`, `Explosion`, `Acid`, `Mental`.
Составной урон и пометки вроде `AreaOfEffect` — через `Of`. Готовые данные заворачивает
обратно в провайдер `Damages.From(data)`.

Смысл против голого конструктора один: вид нельзя забыть и нельзя перепутать местами
с силой отдачи. `new CommonDamage(10f, 0f, DamageType.Fire)` против `Damages.Fire(10f)`.

`Critical` через `Damages` обычно не ставят — его выставляет зональный декоратор при
попадании в критическую зону.

Класс назван `Damages`, а не `Damage`: неймспейсов в SDK нет, а `Damage` уже занято
членами в `DamageBase`, `DamageData`, `TakeDamageEvent` и других — внутри них тип
с таким именем оказался бы затенён.

`new CommonDamage(...)` никуда не делся и работает как раньше.

Если известна точка или конкретный коллайдер попадания, используйте соответствующую перегрузку `TakeDamage()`. Она дополнительно вызовет `OnHitVector` или `OnHitCollider`.

Возможные результаты:

- `NotHandled` — подходящий обработчик не найден;
- `Miss` — попадание отменено или сущность недоступна для урона;
- `Blocked` — урон заблокирован;
- `Damaged` — здоровье уменьшено;
- `Killed` — урон привёл к смерти.

### Удар засчитан?

Почти всем, кто смотрит на результат, нужно не конкретное значение, а один ответ:
прошёл урон или нет. Эффекты попадания, начисление опыта, счётчики ударов одинаково
относятся к `Damaged` и `Killed` — смертельным удар делает вторую половину дела,
но первую он всё равно сделал.

```csharp
if (outcome.IsApplied())
    XPManager.Instance.AddExperiencePoints(reward);
```

Писать пару «или ранил, или убил» руками не нужно: забытая половина означала бы эффект,
который не срабатывает на добивающем ударе — а это как раз тот удар, который игрок ждёт
больше всего.

### Два вопроса, а не один

`IsApplied()` отвечает «удар засчитан». Есть и второй вопрос — «здоровье уменьшилось», —
и это не одно и то же:

| | `outcome.IsApplied()` | `outcome.WasHealthReduced` |
| --- | --- | --- |
| обычная цель | да | да |
| цель с `Is Immortal` | да | **нет** |
| `Miss`, `Blocked`, `NotHandled` | нет | нет |

Груша ради того и сделана: удар по ней засчитывается целиком, эффекты и цифра урона
срабатывают, а тратить нечего.

```csharp
if (outcome.IsApplied())
    PlayHitEffects();                              // отзыв на удар

if (outcome.WasHealthReduced)
    stats.AddDamageDealt(outcome.AppliedDamage);   // прогресс и статистика
```

Второй ответ живёт в `DamageOutcome`, а не расширением перечисления, потому что
по самому `DamageResult` его не получить: груша и обычная цель возвращают одинаковый
`Damaged`.

Правило выбора простое: **эффектам и опыту — `IsApplied()`**, иначе груша перестанет
отзываться. **Прогрессу, достижениям и статистике — `WasHealthReduced`**, иначе по груше
можно будет накрутить «снесено здоровья».

## Данные урона

Для ручного создания данных можно использовать фабричный метод:

```csharp
DamageData data = DamageData.Create(damage: 15f, painShock: 20f);
data.DamageType = DamageType.Fire | DamageType.AreaOfEffect;
data.DamageSource = attacker;

IDamageProvider damage = new CommonDamage(data);
```

`DamageType` является набором флагов. Несколько попаданий одной атаки можно объединить через `CombineDamageData()`, передав общий `DamageId`.

Дополнительные поля результата:

- `RawDamage` — исходное значение до зональных множителей и защиты;
- `AbsorbedDamage` — сколько урона поглотили сопротивления;
- `HitGroup` — зона попадания.

## Урон без оружия

Оружие необязательно: `TakeDamage` принимает `null`, и конвейер его нигде
не разыменовывает — `IWeapon` только доезжает до `DamageHookEvent.Weapon` и событий
как пометка «чем ударили».

Шипы, лава, падение, отравление, таран сущностью — всё это происходит само по себе,
и предъявить там нечего. Для таких случаев есть перегрузки без аргумента оружия:

```csharp
health.TakeDamage(trap, Damages.Of(20f, DamageType.Fall));
hitBox.TakeDamage(spikes, Damages.Common(15f), hit.point);
```

Разница с `TakeDamage(attacker, null, damage)` только в читаемости, но существенная:
`null` посреди вызова выглядит забытым аргументом, а не решением.

Оружие заводят тогда, когда **ударивший предмет нужно назвать отдельно от атакующего**:
брошенный камень, ловушка с именем в списке убийств. Сама сущность-источник в отдельной
обёртке не нуждается — она уже приехала в `attacker`.

## Периодический урон

Горение, отравление, кровотечение — это не декоратор и не модификатор.
`IDamageProvider` описывает разовый расчёт, а тик идёт во времени, прекращается
со смертью цели и переживает того, кто его поставил. Поэтому у эффекта свой жизненный
цикл: `DamageOverTimeCoroutine`.

```csharp
var burn = new DamageOverTimeCoroutine(
    target: health,
    attacker: attacker,
    weapon: weapon,
    damage: Damages.Fire(3f),
    tickInterval: 1f,
    tickCount: 5);

burn.Execute();
```

`tickCount: DamageOverTimeCoroutine.Infinite` тикает, пока эффект не остановят
(`burn.Stop()`) или цель не выйдет из-под урона.

**Время игровое, пауза учитывается.** Отсчёт идёт по `PRTime.GameDeltaTime` и встаёт
на `WaitPause`: замедление растягивает горение вместе с игрой, а открытое меню его
не проматывает и не превращается в лишний тик — ожидание стоит, а не сбрасывается.

**Первый тик приходит через интервал, а не сразу.** Эффект ставят вместе с попаданием,
и мгновенный тик читался бы как двойной урон от одного удара.

**Перед каждым тиком проверяется, можно ли ещё бить.** Цель жива, не блокирует урон,
принимает его (`CanTakeDamage`), а её сущность на сцене и не в пуле. Не прошла проверку —
эффект заканчивается, а не промахивается вхолостую: иначе горение мёртвой сущности
тикало бы до конца сцены, а бесконечное — вечно.

**Владельца корутины можно не задавать.** Тогда она живёт на глобальном хосте и не
обрывается вместе с тем, кто её запустил: ядовитая лужа исчезла, а отравление
продолжается. Уничтоженную цель эффект замечает сам — по Unity-null.

Каждый тик получает свой `DamageId`: общий склеил бы тики в одну серию, как дробь
из одного выстрела. Пометку `DamageType.TimeBased` эффект ставит сам — забыть её нельзя.

## Модификаторы

`MultiplyDamageDecorator` создаёт копию исходных данных, умножает урон и по умолчанию добавляет флаг `Critical`:

```csharp
IDamageProvider baseDamage = new CommonDamage(20f);
IDamageProvider criticalDamage = new MultiplyDamageDecorator(
    baseDamage,
    multiply: 2f,
    addCriticalFlag: true);

target.TakeDamage(attacker, weapon, criticalDamage);
```

Применённый модификатор записывается в `DamageData.AppliedModifiers`, поэтому один идентификатор эффекта не применяется к одним данным повторно. По умолчанию каждый MultiplyDamageDecorator получает собственный Guid: независимые множители перемножаются. Для повторных обёрток одного эффекта передайте одинаковый modifierIdentifier; MultiplyDamageHook хранит такой идентификатор на экземпляре компонента.

## Хуки

Перед проверкой блокировки `HealthComponent` публикует `DamageHookEvent`. Слушатель может:

- заменить урон через `ModifyDamage()`;
- изменить результат через `ModifyResult()`;
- полностью заблокировать попадание через `BlockDamage()`;
- отметить промах через `MissDamage()`.

Хуки следует использовать для брони, иммунитетов, командных правил и других эффектов, которые должны сработать до изменения здоровья.

### Порядок вызова

Слушателей на одну сущность бывает несколько, и порядок между ними важен: отменённый
удар незачем сначала умножать на сопротивления. Задаёт его `Order` — меньшее значение
раньше.

Чтобы числа не выбирались наугад, за ними закреплён смысл:

| Диапазон | Что делает хук | Примеры |
| ---: | --- | --- |
| до −100 | решает, пускать ли удар вообще | `DamageConditionBase` (−200) |
| −100…−1 | отменяет удар по состоянию цели | `InvulnerabilityComponent` (−100) |
| 0 | правит число урона | `DamageResistanceComponent`, `MultiplyDamageHook` |
| больше 0 | смотрит на итог, ничего не меняя | счётчики, логи, аналитика |

Правило простое: **сначала отказ, потом число, потом наблюдение**. Хук, который отменяет
удар, должен отработать до тех, кто считает урон, — иначе их работа пропадает впустую,
а счётчики видят попадания, которых не было.

Внутри одного значения порядок не определён. Двум хукам, которые спорят за одно и то же,
нужны разные `Order`, а не надежда на очередь регистрации.

Отказ выражается через `BlockDamage()` или `MissDamage()`: оба ставят `Supercede`,
и остальные слушатели должны на него смотреть, чтобы не работать вхолостую:

```csharp
public void HandleHook(DamageHookEvent eventArgs)
{
    if (eventArgs.Result == HookResult.Supercede)
        return;
    ...
}
```

## Сопротивления

Добавьте `DamageResistanceComponent` на объект с `EntityBase` и заполните список `Resistances`. Каждое правило содержит маску типов и множитель:

- `0` — полный иммунитет;
- `0.5` — половина урона;
- `1` — без изменения;
- значение больше `1` — уязвимость.

Если в маске правила указано несколько флагов, оно срабатывает при совпадении любого из них. Подходящие правила применяются последовательно, поэтому их множители перемножаются.

Пример настроек:

| Типы | Множитель | Результат |
| --- | ---: | --- |
| `Bullet` | 0.8 | Защита от пуль 20% |
| `Fire` | 0 | Иммунитет к огню |
| `Poison, Radiation` | 1.5 | Уязвимость к яду и радиации |

## События

Локальные события `HealthComponent`:

- `OnHealthChange`;
- `OnEntityDead`;
- `OnRevive`;
- `OnHitVector`;
- `OnHitCollider`;
- `OnSpawn`;
- `OnScaleChanged`.

`OnDamageProcessed` передаёт `DamageOutcome` после любой попытки нанесения урона. Он содержит результат, здоровье до и после, фактически нанесённый и поглощённый урон. Последний результат также доступен через `LastDamageOutcome`.

Глобальные события:

- `IOnTakeDamageEvents.OnTakeDamage()` — применённый урон, включая смертельный;
- `IEntityKillEvent.OnKill()` — убийство сущности уроном;
- `IDamageProcessedEvents.OnDamageProcessed()` — любая завершённая попытка, включая `Miss`, `Blocked` и `NotHandled`.

`TakeDamageEvent` содержит `Outcome`, `Result`, `AppliedDamage` и снимок итогового `DamageData`. `EntityKillEventArgs` также содержит тот же `Outcome`, поэтому обработчику убийства доступны тип урона, зона попадания и фактически снятое здоровье.

Пример глобального слушателя:

```csharp
public sealed class CombatLog : PRMonoBehaviour, IDamageProcessedEvents
{
    public void OnDamageProcessed(DamageProcessedEvent args)
    {
        PRLog.WriteDebug(
            this,
            $"{args.Attacker} -> {args.Victim}: " +
            $"{args.Outcome.Result}, damage={args.Outcome.AppliedDamage}");
    }
}
```

Порядок обработки и уведомлений:

1. Pre-хуки изменяют или отменяют урон.
2. Фиксируются HP, состояние смерти, убийца и `LastDamageOutcome`.
3. Post-хуки получают `DamageHookEvent.Outcome` с окончательным результатом.
4. Вызывается локальный `OnHealthChange`; при смерти — `DeathHandle`, скрытие объекта и `OnEntityDead`.
5. Вызывается локальный `OnDamageProcessed`.
6. Публикуется глобальный `OnTakeDamage`; при смерти — `OnKill`.
7. Публикуется глобальный `OnDamageProcessed`.
8. Перегрузка с точкой/коллайдером вызывает `OnHitVector`/`OnHitCollider`.

Имена событий и их аргументы сохранены. Для совместимости события попадания, как раньше, вызываются для всех результатов, кроме `Miss`, включая `Blocked` и `NotHandled`. Сам факт вызова `OnHitCollider` не означает списания HP — проверяйте результат.

`BlockDamage`, `MissDamage` и обычный `Supercede` подавляют оригинальное действие. Обычный `Supercede` без причины даёт `NotHandled`. У отказов вызываются только локальное и глобальное `OnDamageProcessed` (и событие попадания по правилу выше). Нулевой урон сохраняет прежний результат `Damaged` с `AppliedDamage == 0`; используйте фактический урон, если нужен эффект только при потере HP.

Post-хук предназначен для наблюдения: изменение его `DamageProvider` или `DamageResult` не переигрывает уже завершённую попытку. Используйте `Outcome` как окончательный результат. `StopPropagation` по-прежнему останавливает дальнейшие хуки, включая post-этап, но сам по себе не отменяет оригинальное действие.

Локальные подписчики вызываются независимо: исключение логируется, остальные подписчики и EventBus продолжают получать события. Ошибка хука логируется; уже применённый урон всё равно публикует уведомления. До применения ошибка завершает попытку как `NotHandled`. Последующие хуки после исключения не выполняются.

`DamageOutcome.DamageData` и `TakeDamageEvent.Damage` возвращают защитную копию. Изменение полученного объекта или `AppliedModifiers` не меняет результат для других подписчиков.

## Unity Events в HealthComponent

В Inspector доступен раздел `Unity Events`. Для каждого C#-события есть
сериализованный аналог с суффиксом `Unity`, вызываемый сразу после него:

| UnityEvent | Аргументы |
| --- | --- |
| `OnEntityDeadUnity` | убийца и погибшая сущность (`IEntity`, `IEntity`) |
| `OnReviveUnity` | кто оживил (`IEntity`) |
| `OnSpawnUnity` | позиция (`Vector3`) |
| `OnScaleChangedUnity` | трансформ (`Transform`) |
| `OnHealthChangeUnity` | `HealthChangedEventArgsBase` |
| `OnDamageProcessedUnity` | `DamageOutcome` |
| `OnHitColliderUnity` | атакующий, коллайдер, провайдер урона, результат |
| `OnHitVectorUnity` | атакующий, точка попадания, провайдер урона, результат |

Можно назначать обычные Inspector-действия без аргументов, например запуск звука
или эффекта, либо методы с совпадающей динамической сигнатурой. Интерфейсные
аргументы передаются во время вызова, а не задаются как ссылки в Inspector.
Из кода используется `AddListener`/`RemoveListener`.

Условия срабатывания совпадают с C#-событиями: лечение вызывает изменение здоровья,
а результат `Miss` не вызывает события попадания. Исключение в UnityEvent логируется
и не прерывает последующие этапы обработки урона и CombatEvents. Внутри одного
UnityEvent исключение может прервать оставшиеся обработчики этого UnityEvent —
это стандартное поведение Unity.

## Лечение и возрождение

`AddHealth()` лечит только живую сущность и не превышает `MaxHealth`. При успешном лечении вызывается `OnHealthChange`.

Методы `Revive()` восстанавливают сущность, при необходимости меняют позицию и поворот, включают скрытый при смерти объект и вызывают `OnRevive`.

Место сущности задаётся через `EntityBase.SetPositionAndRotation`, а `Revive()` без аргументов оживляет на месте по `Entity.Position` — не по позиции корня. У сущности-контейнера это разные точки, и оживление переносило бы её к корню (см. «Позиция сущности» в `Core/@Entity/README.md`).

`InitHealth()` выдаёт полное здоровье и **уведомляет подписчиков** через `OnHealthChange`. Этим же методом сущность возвращают на сцену после смерти — из `InitializeEntity()` при восстановлении спрятанных и при выдаче из пула, — а подписки к этому моменту остались с прошлой жизни. Без события полоса здоровья так и показывала бы ноль до первого урона.

## Текущие ограничения

- `TickDamageBrain` даёт только ритм тиков и объявлен `abstract`: кого бить, знает наследник — он же заполняет `Attacker` и `DamageProvider` и наносит урон в `Attack()`. Для урона по одной известной цели объект на сцене не нужен вовсе, это `DamageOverTimeCoroutine`.
- `KnockBackPower`, `PainShock` и `IActiveDamageModifier` хранят данные для внешних обработчиков; `HealthComponent` самостоятельно их не применяет.
- Отрицательные, NaN и бесконечные Damage, RawDamage или AbsorbedDamage отклоняются как NotHandled. Для лечения используйте AddHealth с положительным значением.

## Если дойдёт до оптимизации

Начинать надо **не** с превращения `DamageOutcome` в структуру. Он остаётся классом
намеренно: `null` у него несёт смысл в трёх местах — «хук не дал итога», «эффект ещё
ни разу не играл», «итога нет, отвечаем значением по умолчанию», — и структура так
сказать не может. Полей в нём байт на пятьдесят, так что копирование тоже не бесплатно.

Настоящий расход на одном ударе в другом:

```csharp
var data = context.DamageProvider?.GetDamageData()?.Clone();   // DamageData + HashSet<Guid>
context.Outcome = new DamageOutcome(result, data, ...);        // внутри ещё один Clone
```

`DamageData.Clone()` зовётся дважды и каждый раз создаёт два объекта. Плюс геттер:

```csharp
public DamageData DamageData => damageData?.Clone();
```

Каждое чтение свойства — новая копия. Эффекты читают его, чтобы достать одно число:
`DamagePainShockEffect` — `PainShock`, `DamageKnockBackEffect` — силу отброса. То есть
на каждом попадании там аллоцируется пара объектов ради одного `float`.

Копии не случайны — они защищают данные от правки на стороне, — но дешевле отдавать
готовые значения отдельными свойствами, чем копию всей записи.

## Рекомендации

- Создавайте новый `DamageId` для каждой независимой атаки.
- Указывайте `DamageSource`, если источник важен для эффектов или аналитики.
- Не изменяйте общий `DamageData` из нескольких систем одновременно; используйте `Clone()` или декораторы.
- Проверяйте возвращаемый `DamageResult`, если после попадания требуется отдельная реакция.
