# Ядро

# Зависимости

- newtonsoft.json

# Модули



## PauseManager - Менеджер пауз

## EventBus - Шина событий

## HookSystem - Система хуков.
HookSystem — это система хуков, предназначенная для последовательной обработки событий с возможностью их модификации, отмены или завершения.

В отличие от EventBus, который просто уведомляет подписчиков, HookSystem:
- вызывает слушателей строго по порядку (Order);
- ожидает завершения обработки каждым слушателем;
- позволяет изменить аргументы события;
- позволяет прервать дальнейшую обработку;
- возвращает итоговое состояние события.

## PRTime
PRTime — это модуль управления игровым временем, реализующий поведение, аналогичное UnityEngine.Time, но с поддержкой логической паузы SDK.

## Trackers

- [Документация трекеров](Trackers/README.md)
- Общий доступ через `PRUnitySDK.Trackers`.
- Реестры игроков, сущностей, камер, окон, уведомителей и watcher-свойств.

## Services

- [Документация сервисов](Services/README.md)
- `NameService` — выдача и резервирование случайных уникальных имён.
- `PlayerNameServiceBase` — получение имени текущего игрока из локального или
  платформенного источника.

## CursorManager - Менеджер курсора

- [Документация CursorManager](@Managers/CursorManager/README.md)
- Именованные запросы Show/Hide по `source`: последнее обновлённое активное состояние имеет приоритет.
- Доступ через `CursorManager.Instance`, сохранение состояния — через `ProjectPropertiesManager` и `EnumerationType<bool>`.

## Менеджеры

- [Обзор runtime-менеджеров](@Managers/README.md)
- `GameManager` — загрузка и сохранение данных, autosave и сигнал готовности.
- `ProjectPropertiesManager` — типизированные произвольные свойства проекта.
- `OpenedItemsManager` — учёт открытых предметов.
- `SoundManager` и `AudioMixerManager` — воспроизведение, громкость и mute.
- `PRManagerContainer` — порядок и расширение инициализации менеджеров.



## Последовательность инициализации SDK

- Bootstrap
- SDK
    - > 1 этап инициализации 
    - Конвертеры.
    - Setting
    - Database
    - Trackers
    - Utils
    - PauseManager
    - > 2 этап инициализации Модули
    -
    - > 3 Этап инициализации Менеджеры.
    -
    - Инициализция завершена.
    - > Вызов события завершения инициализации


